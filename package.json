{
  "name": "account-manager",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack account management system",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "node server/dist/server.ts",
    "lint": "eslint ."
  },
  "keywords": [
    "react",
    "express",
    "postgresql",
    "prisma"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.56.0",
    "eslint-plugin-react": "^7.33.2"
  },
  "dependencies": {
    "react-hot-toast": "^2.6.0",
    "vite": "^7.2.2"
  }
}
